{# index.twig #}
{% extends "layouts/layout.twig" %}

{% block page %}
    <header>
        <h1>Quick Gulp set up</h1>
    </header>
    <div class="row">
      <div class="col-md-12">
        <ul>
        {% for value in benefits %}
            <li>{{ value }}</li>
        {% endfor %}
        </ul>
      </div>
    </div>
{% endblock %}
